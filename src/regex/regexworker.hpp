#ifndef REGEX_WORKER_HPP
#define REGEX_WORKER_HPP

#include "automaton.hpp"

namespace regex{

    class RegexWorker{

    public:

        automaton::Automaton getAutomatonFrom(  );

    };
}

#endif //REGEX_WORKER_HPP
